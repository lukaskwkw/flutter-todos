name: todos
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  cool_alert: ^2.0.1
  file_selector: ^1.0.0
  flutter:
    sdk: flutter
  flutter_hooks: ^0.19.0
  fpdart: ^0.6.0
  go_router: ^9.0.3
  provider: ^6.0.5
  shared_preferences: ^2.2.0
  styled_widget: ^0.4.1
  uuid: ^3.0.7
  whisper_dart: ^0.0.11
  whisper_flutter: ^0.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
